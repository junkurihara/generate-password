# pnpm configuration (pnpm v11 reads settings from this file, not package.json#pnpm)
# unrs-resolver ships prebuilt binaries; its postinstall build script is unnecessary.
allowBuilds:
  unrs-resolver: false
